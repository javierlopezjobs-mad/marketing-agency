# Runtime dependencies
crewai>=0.30.0
crewai-tools>=0.14.0
python-dotenv>=1.0.0

# Dev
pytest>=7.0.0

# Optional LLM provider extras (see .env.example) — not required by default:
# litellm                    # non-native providers, e.g. groq/*
# crewai[google-genai]       # gemini/* models
